From: Kill Your TV <killyourtv@i2pmail.org>
Date: Tue, 7 Oct 2014 14:13:54 +0000
Subject: set version

Later will do this automatically using the version in debian changelog. But for now...

---
 cfg/global_vars.mk | 2 +-
 cfg/targets/doc.mk | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

--- a/cfg/global_vars.mk
+++ b/cfg/global_vars.mk
@@ -1,11 +1,6 @@
 # Version
 TOXIC_VERSION = 0.5.2
-REV = $(shell if which git &>/dev/null ; then git rev-list HEAD --count 2>/dev/null || echo -n "error" ; else echo -n "error" ; fi)
-ifneq ($(REV), error)
-	VERSION = $(TOXIC_VERSION)_r$(REV)
-else
-	VERSION = $(TOXIC_VERSION)
-endif
+REV = 1385
 
 # Project directories
 DOC_DIR = $(BASE_DIR)/doc
